/* istanbul ignore file */
import InputGroupLabel from './src/InputGroupLabel.vue'

InputGroupLabel.install = function (Vue) {
  Vue.component(InputGroupLabel.name, InputGroupLabel)
}

export default InputGroupLabel
