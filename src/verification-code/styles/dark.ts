import type { VerificationCodeTheme } from './light'
import { commonDark } from '../../_styles/common'
import { self } from './light'

const verificationCodeDark: VerificationCodeTheme = {
  name: 'VerificationCode',
  common: commonDark,
  peers: {
  },
  self
}

export default verificationCodeDark
