import { c, cB } from '../../../_utils/cssr'

export default c([
  cB('mermaid', `
        width: 100%;
        height: auto;
    `),
  cB('mermaid-contain', `
        width: 100%;
        height: auto;
        overflow: auto;
    `)
])
