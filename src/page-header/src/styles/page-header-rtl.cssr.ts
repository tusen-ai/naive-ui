import { c, cB, cM } from '../../../_utils/cssr'

export default c([
  cB('page-header-header', [
    cM('rtl', `
      direction: rtl;
    `)
  ]),
  cB('page-header', [
    cM('rtl', `
      direction: rtl;
    `)
  ]),
  cB('page-header-content', [
    cM('rtl', `
      direction: rtl;
    `)
  ]),
  cB('page-header-footer', [
    cM('rtl', `
      direction: rtl;
    `)
  ])
])
