import { commonDark } from '../../_styles/common'
import type { NumericAnimationTheme } from './light'
import { self } from './light'

const numericAnimationDark: NumericAnimationTheme = {
  name: 'NumericAnimation',
  common: commonDark,
  self
}

export default numericAnimationDark
